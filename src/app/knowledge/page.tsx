import {AppShell} from "@/components/AppShell";import {garments,fabrics,contexts,aesthetics} from "@/lib/knowledge";
const groups=[['Garments',garments],['Fabrics',fabrics],['Contexts',contexts],['Aesthetics',aesthetics]] as const;
export default function Knowledge(){return <AppShell><section className="knowledgeHero wrap"><p className="eyebrow">PHASE 1 · FASHION KNOWLEDGE CORE</p><h1>The atelier now has a vocabulary.</h1><p>Structured garment, fabric, context and aesthetic records are now machine-readable and versioned.</p></section><section className="knowledge wrap">{groups.map(([label,items])=><article key={label}><div className="groupHead"><h2>{label}</h2><span>{items.length} records</span></div><div className="knowledgeGrid">{items.map((x,i)=><div className="knowledgeItem" key={x}><span>{String(i+1).padStart(2,'0')}</span><h3>{x}</h3><p>Curated Phase 1 knowledge record.</p></div>)}</div></article>)}</section></AppShell>}